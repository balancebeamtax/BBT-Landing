// Fixed effective date for the legal pages (Privacy, Terms). Hard-coded so the
// stamp never drifts on redeploy — bump it deliberately when the copy changes.
export const EFFECTIVE_DATE = "June 20, 2026";
